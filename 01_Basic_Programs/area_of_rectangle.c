// calculate the area of a rectangle
#include<stdio.h>
void main()
  {
    float l,w,area;
	printf("Enter the value of l and w");
	scanf("%f%f",&l,&w);
	area = l*w;
	printf("area of rectangle = %f",area);
}
